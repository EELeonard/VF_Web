import type { Metadata } from "next";
export const metadata: Metadata = { title:"Book a Flight | Vienna Flight", description:"Select a released Vienna Flight simulator appointment online." };
export default function EnglishBookingLayout({ children }: { children: React.ReactNode }) { return children; }
